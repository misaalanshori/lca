# Activated

The workflow now lives in `.github/workflows/ci.yml`; this directory keeps
the history of the credential-scope blocker that parked it during early
build-out (fine-grained PAT without the Workflows permission, since
replaced).
